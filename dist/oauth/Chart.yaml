# Helm versioning - v2 = >= helm-v3
apiVersion: v2

# Chart name/version
name: Auth API
version: 0.1.0
description: The Credential and Authorization API service for oAuth management and token issuing
keywords:
  - Auth
  - Credentials
  - oAuth
  - Facebook
  - Google
maintainers:
  - name: Christian Shaw
    email: chris@koda.software
# App version
appVersion: 0.1.0

# Kubernetes version
kubeVersion: ">= 1.16.0"
